{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && (git diff --cached --quiet || (git commit -m \"auto: $(git diff --cached --name-only | head -5 | tr '\\n' ' ')\" && git push origin main)) || true",
            "statusMessage": "Committing and pushing changes..."
          }
        ]
      }
    ]
  }
}
